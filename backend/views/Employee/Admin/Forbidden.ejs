<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Access Denied - Admin</title>
    <style>
        body {
            background: #fff5f5;
            color: #333;
            font-family: 'Segoe UI', Arial, sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
        }
        .forbidden-container {
            background: #ffe6e6;
            border: 2px solid #ff6b6b;
            border-radius: 16px;
            box-shadow: 0 4px 24px rgba(0,0,0,0.07);
            padding: 48px 36px 36px 36px;
            text-align: center;
            max-width: 400px;
        }
        .forbidden-icon {
            font-size: 54px;
            color: #e74c3c;
            margin-bottom: 18px;
        }
        .forbidden-title {
            font-size: 2em;
            font-weight: bold;
            color: #c0392b;
            margin-bottom: 10px;
        }
        .forbidden-message {
            font-size: 1.1em;
            margin-bottom: 28px;
            color: #555;
        }
        .forbidden-btn {
            background: #e74c3c;
            color: white;
            border: none;
            border-radius: 6px;
            padding: 12px 32px;
            font-size: 1em;
            font-weight: 500;
            cursor: pointer;
            transition: background 0.2s;
        }
        .forbidden-btn:hover {
            background: #c0392b;
        }
    </style>
</head>
<body>
    <div class="forbidden-container">
        <div class="forbidden-icon">🔒</div>
        <div class="forbidden-title">Access Denied</div>
        <div class="forbidden-message">
            Sorry, you do not have permission to view this page.<br>
            Please contact your system administrator if you believe this is a mistake.
        </div>
        <button class="forbidden-btn" onclick="window.location.href='/Employee/AdminIndex'">Go to Admin Dashboard</button>
    </div>
</body>
</html>
